# SCT_ML_Task3
This project implements a Support Vector Machine (SVM) model to classify images of cats and dogs using the Kaggle dataset. It includes image preprocessing, feature extraction, model training, and evaluation to accurately distinguish between the two classes.
